import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  colors: {
    discordGrey: {
      100: '#424549',
      200: '#36393e',
      300: '#282b30',
      400: '#1e2124',
    },
  },
})

export default theme
